import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.prakhar.college.timetable',
  appName: 'Prakhar Timetable',
  webDir: 'dist'
};

export default config;
