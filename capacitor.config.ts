import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.prakhar.college.timetable',
  appName: 'Timetable',
  webDir: 'dist'
};

export default config;
